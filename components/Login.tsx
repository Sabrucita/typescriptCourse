/*import React from "react";
import CustomInput from "./CustomInput";
import clientType from './../helpers/clientType';

interface Props {
  clients: clientType,
}

const Login: React.FC<Props> = ({clients}) => {
return(
		<CustomInput clients={clients} />
	)
}

export default Login;*/